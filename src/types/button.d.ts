export interface ButtonParams {
  title?: string,
  text?: string,
  tts?: string,
  url?: string,
  hide?: boolean,
  payload?: {}
}

export interface Button {
  title: string,
  tts?: 
  url:
  hide: boolean
  payload?: {}
}